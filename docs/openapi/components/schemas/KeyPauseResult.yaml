type: object
required: [result, key]
properties:
  result: { type: string, enum: [paused, already_paused, resumed, not_paused, not_found, disabled] }
  key:
    type: object
    required: [fingerprint]
    properties:
      fingerprint: { type: string }
      pausedAt: { type: integer, format: int64 }
